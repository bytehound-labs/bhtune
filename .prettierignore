# Generated by Cargo
target/

# Node / pnpm (frontend/)
node_modules/
dist/
.pnpm-store/

# Auto-generated by pnpm; reformatting it would just be undone on the next
# `pnpm install`/`pnpm add`. pnpm, not Prettier, owns this file's formatting.
pnpm-lock.yaml

# Generated by `cargo run -p bhtune-server --example gen_openapi` from the utoipa
# route/DTO annotations (see that file's doc comment). Its own `git diff --exit-code`
# CI check is the source of truth for staleness, not Prettier -- utoipa's JSON
# serializer has its own (different, and equally valid) formatting conventions.
openapi.json
