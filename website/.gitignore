# Dependencies
/node_modules

# Production
/build

# The Rust API reference (rustdoc, `docs-api-rustdoc`) is generated by
# .github/workflows/docs-deploy.yml and copied here right before the Docusaurus
# build, which copies static/ verbatim into build/ -- never hand-authored or committed.
/static/api

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
